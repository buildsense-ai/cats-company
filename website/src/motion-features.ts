import { domAnimation } from 'framer-motion'

// Feature set for LazyMotion: covers animations, variants, whileInView and
// whileHover. No drag or layout animations are used on this site, so the
// smaller domAnimation set is enough. Loaded asynchronously from App.tsx.
export default domAnimation
